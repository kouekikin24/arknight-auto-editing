# 上游修复 PR 草案（liemark/arknight-auto-editing）

> 状态：**草案待审，未提交、未推送**。目标仓库：`liemark/arknight-auto-editing`（main @ ce10360 = v26.7.23）。
> 本文所有数据均为 fork 侧实测（样本：`D:\qq下载\920\4.mp4`，118 分钟、424,176 帧、VFR、
> 用户真实逐帧级剪法 13,619 保留段）。参照实现：fork 提交 `95b2c30`（音频分批 + 哨兵修复）、
> `47645ee`（视频分批 + 预检豁免）；调研报告 `RESEARCH_PHASE3_20260831.md`。

---

## 标题

fix: 大段数剪辑导出卡死/无声——音频分批 + 视频分批 + 失败保护

## 问题现象

保留段数较大时（本例 13,619 段；早期清单 2,623 段），导出在滤镜图阶段卡死：
v26.7.23 实测 3 小时无进展（超时 1800s 后删除半成品、静默回退逐帧路径，长源片上
逐帧路径本身是小时级）。GUI 默认勾选"保留音频"，普通用户必踩。

## 根因（四条，全部有实测）

1. **音频 atrim/concat 图随段数超线性爆炸**（主凶）。fork 同款结构实测：
   400 段 ≈120s（≈纯音频解码地板）、800 段 >600s 被杀、2,623 段 0 进展。
   建图本身也爆炸：2,623 段图纸只喂 60s 源片仍 >180s。
2. **视频 trim/concat 逐段链同样平方级增长**（`_export_ranges_with_ffmpeg_filters`
   当前结构，注释"ffmpeg concat 可处理上千段，因此不再对段数设上限"不成立）。
   合成 CFR 源实测：只喂 300 帧时 N 翻倍耗时 ×3.4~×4.0（纯图规模病）；
   按 13,619 段 × 42 万帧外推为数小时。
3. **失败路径不友好**：`subprocess.run(timeout=1800)` 超时后 `os.remove(output_path)`
   删半成品 + 静默回退逐帧路径，用户只能看到"卡住"。
4. **潜伏的音画错位风险**：视频用 `start_frame/end_frame`（帧号寻址）、音频用
   `start={start/fps}`（帧率折算秒数）——VFR 源上两个时间系不一致。fork 用认证
   PTS 时间表解决；上游没有这套机制，至少在文档/注释中标注为已知限制。

## 修复方案（最小侵入，段数小时行为完全不变）

1. **音频分批**（段数 >400 时）：视频趟 `-an`；音频每 ≤100 段一批——批内
   atrim/concat → 无损 PCM（`pcm_s16le` 封装 .nut）→ concat demuxer（除末批外写
   精确 `duration` 指令）→ 混流时一遍 AAC。
   实测：批边界漂移 **±0 样本**（对比单趟图）；`atrim start/end` 与样本对齐精确。
   注意必须加**输入侧** `-t {批跨度}`，否则每批解码到 EOF（楼梯形浪费）。
   参照：fork `analyzer._export_audio_pts_batched`（`95b2c30`）。
2. **视频分批**（段数 >4000 时）：每 ≤2000 段一批——`-ss {批首-1s}` 寻址 +
   `-copyts` 保原始时间戳 + 批内滤镜 + `-frames:v {批帧数}` 输出上限 →
   concat demuxer（精确 `duration`）`-c copy` 拼接。
   ⚠️ 若想改用"平铺 select 单表达式"替代 trim 链：注意 FFmpeg 表达式求值栈在
   4,000~13,619 项之间溢出（实测 13,619 项 `0xC00000FD` 秒崩，4,000 项探针通过），
   万段级仍需分批。
3. **失败保护**：快速路径失败/超时时保留（无声）视频产物并在状态行说明原因，
   不删半成品；删除静默回退逐帧的行为（或改为显式确认）。
4. **文档修正**：把"concat 可处理上千段，不设上限"的注释改为真实上限
   （音频图 ≈400 段、视频链为平方级增长）。

## 验证数据（fork 侧，可复现）

- 13,619 段 / 424,176 帧 / 带音频：CPU 7.4 分钟、NVENC 约 8 分钟完成；
- 输出帧 PTS 与源认证时间表逐帧一致（20,428/20,428）；
- 音频 137 批：长度差 +640 样本（AAC 帧填充量级，容差 ±2048），非静音；
- 产物容器声明时长 = 末帧真实结束位（无幻影包；fork 另有 tpad 克隆哨兵的
  setts bsf 修复，上游若引入 PTS 时间轴时建议一并参考）。

## 回归测试建议

- 段数缩放曲线：同一长源上 N ∈ {100, 400, 800, 2000, 4000} 计时，断言近似线性；
- 音频 A/B：≤400 段时单图 vs 分批，解码后样本级比对（应逐样本一致）；
- 失败注入：故意构造音频图失败，断言产物仍在、状态行有原因。

## 血统备注（供 maintainer 参考）

音频导出第一版（PR #2，concat demuxer + inpoint/outpoint）架构是健康的；
`224040d` 删除后由 PR #4（atrim/concat 图）重写，病由此引入。本 PR 的音频分批
实质是"回到 PR #2 的分治思想 + PCM 中间件保证批边界样本级精确"。
