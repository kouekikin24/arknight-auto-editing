# PTS 瑕疵事实报告（只读证据分析）

生成时间（UTC）：2026-08-16T07:37:56.784875+00:00

数据来源：既有 frame oracle（完整 PTS 表）与业务 skip 段表；未重新扫描任何媒体。

## 结论

- 全部 PTS 瑕疵帧是否都位于删除区内：否
- 瑕疵帧到最近切点边界的最小距离：0 帧
- 瑕疵 tick 与任何裁剪边界 tick 的最小差距：0.000000 ms
- tick 裁剪无法正确表示的保留帧总数（即真实导出损伤）：1 帧

## 分样本明细

| 样本 | 帧数 | skip 段数 | 瑕疵帧范围 | 最小距离(帧) | 最小 tick 差距(ms) | 全部在删除区 |
|---|---:|---:|---|---:|---:|---|
| 1 | 249097 | 2311 | [5,9] | 979 | 16316.666667 | 否 |
| 2 | 184293 | 683 | [1,5] | 0 | 0.000000 | 否 |
| 3 | 29804 | 108 | [3,7] | 3 | 50.000000 | 是 |
| 4 | 424176 | 2623 | [3,7] | 3 | 50.000000 | 是 |

## 瑕疵帧明细

### 样本 1(time_base 1/15360,fps 60.000)

- 帧 5(pts=1280, 0.083333s): 区域=kept(段 -), 最近边界=帧 988(距离 983 帧 / 16.383s), 边界 tick 差 16350.000000 ms
- 帧 6(pts=1792, 0.116667s): 区域=kept(段 -), 最近边界=帧 988(距离 982 帧 / 16.367s), 边界 tick 差 16316.666667 ms
- 帧 7(pts=1280, 0.083333s): 区域=kept(段 -), 最近边界=帧 988(距离 981 帧 / 16.350s), 边界 tick 差 16350.000000 ms
- 帧 9(pts=1792, 0.116667s): 区域=kept(段 -), 最近边界=帧 988(距离 979 帧 / 16.317s), 边界 tick 差 16316.666667 ms
- 导出损伤：无（所有瑕疵帧均可被 tick 裁剪正确表示）

### 样本 2(time_base 1/15360,fps 59.999)

- 帧 1(pts=256, 0.016667s): 区域=kept(段 -), 最近边界=帧 5(距离 4 帧 / 0.067s), 边界 tick 差 33.333333 ms
- 帧 2(pts=768, 0.050000s): 区域=kept(段 -), 最近边界=帧 5(距离 3 帧 / 0.050s), 边界 tick 差 0.000000 ms
- 帧 3(pts=256, 0.016667s): 区域=kept(段 -), 最近边界=帧 5(距离 2 帧 / 0.033s), 边界 tick 差 33.333333 ms
- 帧 5(pts=768, 0.050000s): 区域=deleted(段 [5,5139]), 最近边界=帧 5(距离 0 帧 / 0.000s), 边界 tick 差 0.000000 ms
- **导出损伤**：保留帧 2(pts=768) 位于保留区间 [0,5) 的 tick 端点 [0,768) 之外——纯 tick 裁剪无法既保留它又剔除同 tick 的删除帧

### 样本 3(time_base 1/15360,fps 59.996)

- 帧 3(pts=768, 0.050000s): 区域=deleted(段 [0,10653]), 最近边界=帧 0(距离 3 帧 / 0.050s), 边界 tick 差 50.000000 ms
- 帧 4(pts=1280, 0.083333s): 区域=deleted(段 [0,10653]), 最近边界=帧 0(距离 4 帧 / 0.067s), 边界 tick 差 83.333333 ms
- 帧 5(pts=768, 0.050000s): 区域=deleted(段 [0,10653]), 最近边界=帧 0(距离 5 帧 / 0.083s), 边界 tick 差 50.000000 ms
- 帧 7(pts=1280, 0.083333s): 区域=deleted(段 [0,10653]), 最近边界=帧 0(距离 7 帧 / 0.117s), 边界 tick 差 83.333333 ms
- 导出损伤：无（所有瑕疵帧均可被 tick 裁剪正确表示）

### 样本 4(time_base 1/15360,fps 60.000)

- 帧 3(pts=768, 0.050000s): 区域=deleted(段 [0,2179]), 最近边界=帧 0(距离 3 帧 / 0.050s), 边界 tick 差 50.000000 ms
- 帧 4(pts=1280, 0.083333s): 区域=deleted(段 [0,2179]), 最近边界=帧 0(距离 4 帧 / 0.067s), 边界 tick 差 83.333333 ms
- 帧 5(pts=768, 0.050000s): 区域=deleted(段 [0,2179]), 最近边界=帧 0(距离 5 帧 / 0.083s), 边界 tick 差 50.000000 ms
- 帧 7(pts=1280, 0.083333s): 区域=deleted(段 [0,2179]), 最近边界=帧 0(距离 7 帧 / 0.117s), 边界 tick 差 83.333333 ms
- 导出损伤：无（所有瑕疵帧均可被 tick 裁剪正确表示）

## 交叉核验

每个样本用完整 PTS 表重新计算瑕疵并与 oracle 自报计数比对
（oracle 计重复组数，重算计涉及帧数；非单调为步数，口径一致）：

- 样本 1: oracle 报重复 2/重算 4, 非单调 1/重算 1
- 样本 2: oracle 报重复 2/重算 4, 非单调 1/重算 1
- 样本 3: oracle 报重复 2/重算 4, 非单调 1/重算 1
- 样本 4: oracle 报重复 2/重算 4, 非单调 1/重算 1
