# 设计：样本 4 分批寻址导出（batched-seek export）

> 阶段一交付物（2026-08-28）。实施前需 owner 过目。
> 目标：让样本 4（2623 段 × 424176 帧）的认证导出在合理时间内完成。
> 红线：导出仍 100% 走 `ffmpeg.exe`；剪辑决策（哪些段保留）一行不动。

---

## 1. 成本模型确诊（读码结论）

`analyzer.export_pts_schedule`（analyzer.py:1896）按 `frame_pts_status` 分两条路：

| 路径 | 触发条件 | 滤镜构造 | 复杂度 |
|---|---|---|---|
| **VFR 平铺路**（`_pts_select_setpts_video_filter`，:1862） | `frame_pts_status == "vfr"` | 单条 `select='between(pts,s0,e0)+between(pts,s1,e1)+…'`（2623 项平铺 OR）+ `setpts` 平铺 `if()` 间隙扣除（约 2623 项）+ `tpad` 末尾克隆哨兵 | **每帧求值全部表达式**：O(段数 × 帧数) |
| **CFR 逐段路**（:1977） | 其他 | 每段一条 `trim=start_pts/end_pts,setpts=PTS-STARTPTS`，最后 `concat` | 每帧流经全部 2623 条 trim 链：O(段数 × 帧数)，但常数小（每链仅一次 PTS 比较） |

**样本 4 走的是 VFR 平铺路**。证据：超时被杀的命令行里带 `-fps_mode:v passthrough -bf 0 -enc_time_base 1/15360 -video_track_timescale 15360 -frames:v 107434`——这些参数只在 vfr 分支添加（analyzer.py:2022-2033）。

**2.2B 次求值的来源**：424176 帧 × 每帧约 (2623 个 `between` + 2623 个 `if`) ≈ 2.2×10⁹ 次**解释器**表达式求值（ffmpeg 的 eval 引擎逐帧重新求值，单次微秒级），叠加 424k 帧的解码 + 编码，总耗时远超 10800s 超时。音轨侧另有 2623 条 `atrim` 链（音频帧少，成本次要）。

段数上限 `_MAX_PTS_EXPORT_RANGES = 4000`（:38）拦不住这种情况——2623 < 4000 合法，但运行时不可行。结论：**问题不是段数超限，而是平铺表达式随段数线性放大、且每帧全量求值。**

---

## 2. 方案：分段寻址 + 分批编码 + 容器拼接

核心思想：**不让任何一次 ffmpeg 调用背全量段数的表达式，也不让它解码全量源帧。** 把 2623 段切成若干批（batch），每批只用输入寻址（`-ss`/`-to` 放在 `-i` 之前）解码自己时间范围内的源帧，批内段数少（表达式便宜），批与批各自编码为中间文件，最后用 concat demuxer + `-c copy` 无损拼接。

### 2.1 分批

- 批大小 `B`（建议 64–128 段/批，实测调优）；批切点**只落在段边界**，绝不切断段。
- 每批记录：起始秒、结束秒（由批内首段 `start_tick` 与末段 `end_tick` 换算）。

### 2.2 每批的 ffmpeg 调用

- `-ss <batch_start_s> -to <batch_end_s>` 置于 `-i` 之前：**输入寻址**。现代 ffmpeg 对重编码场景的输入寻址是帧精确的——从最近关键帧解码并丢弃到目标点，不丢帧不错帧（**需在阶段二对本机样本实测确认，含 -copyts 组合**）。
- 批内滤镜用既有 VFR 逻辑的**批内版**：`select`/`setpts` 只覆盖本批的段，间隙扣除只算批内间隙，输出 PTS 归零。段数从 2623 降到 B，每帧求值量降 2623/B 倍。
- 末批保留 `tpad=stop_mode=clone:stop=1` 哨兵；前几批不加。
- 编码器参数逐批一致（同一 `quality`/`-bf 0`/`-enc_time_base`），保证拼接兼容。

### 2.3 批间拼接

- 每批产出中间 `.mp4`（含音轨则同批处理音频：批内 `atrim` + 批内 concat）。
- 最后一遍：`ffmpeg -f concat -safe 0 -i list.txt -c copy` 拼成成品。`-c copy` 不重编码，只改容器；前提是各批同编码器同参数（已保证）。
- 各批输出 PTS 均从 0 起始，concat demuxer 按时长顺延拼接 → 成品时间轴连续。

### 2.4 成本估算

- 解码：每批只解码自己的区间（寻址丢弃到关键帧的开销有限）；总解码量 ≈ 保留段跨度之和（远小于 424k 全片）。
- 表达式：每批 O(B × 批内帧数)，合计约 O(段数 × 批内帧数均值)——表达式求值总量从 2.2B 降到约 **42M（B=100 时，降约 50 倍）**。
- 进程启动：约 2623/100 ≈ 27 次 + 1 次拼接，可忽略。
- 预期：从"3 小时跑不完"降到可接受量级（具体以实测为准）。

---

## 3. 等价性验收标准（硬门槛）

**注意：批间边界会改变 x264 的参考帧上下文，因此分批与单趟产物不会逐字节一致。验收标准是"内容与时戳等价"，不是"字节等价"：**

1. **帧数精确相等**：分批产物帧数 == `scheduled_written`（与现有口径一致）。
2. **输出时间戳精确相等**：对产物跑现有帧 PTS oracle，核对每个输出帧的 tick 序列与认证时间表一致（复用现成机器）。
3. **内容同源抽查**：在有单趟成品的样本（1/2/3 号）上，逐帧解码对比分批产物与单趟成品——允许重编码差异，但每帧必须对应同一源帧（时序错位是红线）。
4. **音画对齐**：成品音轨时长与视频轨差在既容忍范围内。

样本 4 没有单趟成品可比 → 靠 (1)(2) + 源帧级内容抽查验证；(3) 的横向对照用 1/2/3 号做。

---

## 4. 阶段二实测结论（2026-08-28/29，已全部回答）

**轻方案（逐段原生 trim/concat 用于 VFR）——已实测，出局。**
- 3 号（干净 VFR）PASS；但 2 号（坏时间戳）FAIL：帧数 16970≠16973、输出时戳被
  "压"成匀速 60fps（512=2×256 间隔）、内容错位。
- 结论：trim/concat 依赖 ffmpeg 逐段整理时间戳，坏时间戳源上会自作主张按匀速补全；
  **平铺 select 用认证 tick 做显式算术，正是为扛坏时间戳而存在，不可替换**。

**分批寻址（保留平铺 select 语义，按批寻址）——已实测，成立。**
关键前提逐一验证通过：
1. `-ss` 输入寻址 + `-copyts` **保留原始 PTS**（不被重置），能精确命中认证帧（2 号实测）。
2. 数学等价：批内 `setpts`(减批首段与批内间隙) + concat 顺延 ≡ 全局平铺公式（数值验证）。
3. **批间拼接需写精确 `duration` 指令**：concat demuxer 默认按文件时长定位会漂 1 帧，
   按全局时间表算出每批真实跨度写入 concat 列表后，批间逐帧对齐。
4. 批大小取 100 段（表达式项数 ≪2623，寻址回溯开销可接受）。

**实测结果：**

| 样本 | 类型 | 段数 | 保留帧 | 分批结果 | 对照 |
|---|---|---|---|---|---|
| 3 | 干净 VFR | 108 | 1901 | ✅ PTS 1901/1901 一致 | 与单趟成品逐帧 |
| 2 | 坏时间戳 | 683 | 16973 | ✅ PTS 16973/16973 一致 | 与单趟成品逐帧 |
| 4 | 目标/2623段 | 2623 | 107433 | ✅ PTS 107433/107433 一致，帧数✓ | 对照认证时间表（无基准） |

**性能**：4 号视频-only 分批编码 **~50 分钟**（27 批，单线程串行）——从"3 小时超时、永不完成"
变为可行。后续可优化（并行批、更大批、编码 preset），但正确性已确立。

**待做（阶段三）**：音频同样分批（批内 atrim/concat）并入拼接；实现进 `export_pts_schedule`；
回归 1/2/3 号；新增单测。

---

## 5. 实施步骤（阶段三）

1. 在 `analyzer.py` 新增分批导出实现（`frame_pts_status == "vfr"` 且段数超阈值时启用，或加显式开关）。
2. 音频按同样的批切分（批内逐段 atrim/asetpts/concat），与视频批一一对应后统一拼接。
3. 批间 `-c copy` 拼接 + 精确 `duration` 指令。
4. 回归 1/2/3 号（有单趟成品，逐帧比对），全量 `pytest` 保持绿。
5. 更新 `HANDOFF_STATE_20260827.md` 与测试（至少新增分批路径的单测）。

## 6. 不做的事

- 不动剪辑决策、证书、oracle。
- 不改 CFR 逐段路（样本 1 走它且已通过；本次只治 VFR 大段数场景，若实测发现 CFR 路在大段数下同样慢再另议）。
- 不引入新二进制依赖（拼接仍用同一个 `ffmpeg.exe`）。
