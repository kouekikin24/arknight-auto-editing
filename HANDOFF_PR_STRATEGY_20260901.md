# HANDOFF — PR 策略与可行性分析（2026-09-01，分叉会话）

> 导出专项全史见 `HANDOFF_EXPORT_PERF_20260830.md`（HEAD=`fe84167`，工作区干净）。
> **本次会话纯分析、无新代码提交**：全量 PR 可行性、上游 PR 景观调研、64k 差异分解。
> **待决两件事：①分支推 fork 备份（等 owner 点头）②裁剪版修复 PR 实施（等草案审过）。**

---

## 1. PR 可行性结论（含一处已修正的误判）

- 本地历史是**独立根**：b706823 是真根提交（非浅克隆截断——`.git/shallow` 只含
  8f2dc2e=origin/main 一处）。
- **PR#9 被上游 rebase 合并**：本地 `dc6259a`→上游 `e9f4de1` 等，内容相同 SHA 全异
  → 两边历史无共享提交对象。
- ⚠️ **已修正的误判**：早前断言"GitHub 无法对无共同祖先的分支开 PR"是错的——PR#9
  本身就是反例（GitHub 对无关历史做树级 diff 并可合并）。owner 指正后修正。
- **全量 PR 实际不可行的真实原因**（与机制无关）：
  ① 体量 +64,301/−3,305（123 文件），无维护者能审；
  ② 树级 diff 会把上游后来合并的工作（PR#4 等）显示为"删除"——合并=回退上游；
  ③ 音频部分是两套互斥架构（见 §4），merge 工具无法自动裁决。

## 2. 上游 PR 景观（gh 实测，截至 2026-09-01）

| PR | 状态 | 规模 | 题材 |
|---|---|---|---|
| #9 | 合并 | 4,870 行 | A_PT（owner 的，历史最大） |
| #6 | 合并 | +790 | uv 打包 |
| #4 | 合并 | +508 | 导出修复（**埋雷版**，atrim 图 800 段即爆炸） |
| #2 | 合并 | +367 | 音频处理 |
| #5 | 开放 | +130 | macOS CI |

维护者口味：单题材、几百行是甜区、历史极限 <5k 行；响应积极（4/5 合并）。
**卖点定位**：PR#4 标题即"修复导出卡死"但实测没修好——我们的修复 PR 是带实锤
数据的续集，接受度预期高。

## 3. 64k 差异分解（`git diff liemark/main HEAD --stat` 实测）

| 部分 | 行数 | PR 去向 |
|---|---|---|
| scripts/ 验证基准工具 | +27,549 | ❌ 内部产物 |
| tests/ fork 架构测试 | +15,650 | ❌ 架构不推则测试无从推 |
| *.md 交接/调研文档 | +5,023 | ❌ 工作记录 |
| 核心新模块（PTS认证链/mpv引擎/任务架构） | +6,691 | ⚠️ 需先对话 |
| 共享文件（analyzer/preview_player 等 6 个） | +8,308/−2,971 | 部分含可推修复 |
| 修复 PR（音频分批+视频分批+失败保护） | ~500-800 行（移植后） | ✅ 立即可推 |

**结论：~1% 马上可推、~20% 谈妥再推、~75% 永不推（内部底座不是贡献）。**

## 4. 两套音频解法（PR 里最核心的技术冲突）

- **上游（PR#4）**：帧号切视频 + "帧号÷帧率"折算秒数切音频（两个时间系，VFR 源
  音画错位风险；>800 段图爆炸——我们实测）。
- **fork**：认证 PTS 时刻表单一时间系；视频平铺 select（>4000 段分批寻址+精确拼接）；
  音频 >400 段分批 PCM 中间件；失败降级不删产物。
- 一个导出函数只能留一套寻址哲学——这是"合并不了只能拍板"的具体含义。

## 5. 下一步（按优先级）

1. **备份**（等 owner 点头）：`git push origin fix/preview-pacing-metrics`——
   65 提交目前仅存本机；纯新增分支、不覆盖任何东西。
2. **修复 PR 实施**（等草案 `UPSTREAM_FIX_PR_DRAFT.md` 审过）：上游克隆
   （`D:\ArknightsPathFinding\arknight-auto-editing-upstream` @ ce10360）开分支 →
   移植三条修复（音频 >400 段分批 PCM / 视频 >4000 段分批 / 失败降级不删产物，
   参照 fork `95b2c30`+`47645ee`）→ 上游环境跑缩放验证（400/800/2623 段计时 +
   音频样本级 A/B）→ 验证数据回填草案 → **做到开 PR 前一步停手，owner 按键**。
3. **UI 继续演进**（owner 进行中，明确不在任何 PR 计划内）。
4. 修复 PR 若被合 → 开 issue 探维护者对架构的兴趣 → 再决定要不要做全量移植
   （路线 A：上游为底+移植，约 1-2 天 + 全套重验；路线 B：合流，同等工程量，不推荐——
   会把探索史缠进上游历史）。
5. 可选：并行批编码（已实测 x264 确定性支持，~再快一倍）/ OpenCV 5 对齐验证。

## 6. 仍在生效的铁约束（沿用 HANDOFF_STATE_20260827 §6）

不直接 push upstream；不用 `git reset --hard`/`checkout --`/`clean`；owner 说停立即停
且清后台进程；不重跑四个完整 frame oracle 长扫描；不动 10ms 内容阈值；
禁止 frame/fps 时间戳回退造正式 EDL；CvEngine/VideoIOThread 保留；
解码后端优先 A_PT；导出与帧 oracle 留 ffmpeg.exe；等价性是硬门槛。

## 7. 关键位置

- 交接主文档：`HANDOFF_EXPORT_PERF_20260830.md`（导出专项全史，含 §2.5 万段分批、
  §2.6 B帧+音频对齐）
- 调研报告：`RESEARCH_PHASE3_20260831.md`；PR 草案：`UPSTREAM_FIX_PR_DRAFT.md`
- 样本：`D:\qq下载\920\{1,2,3,4}.mp4`；基准 `.cache/research_phase3/A2a_singlepass_v2623.mp4`（勿删）
- ⚠️ 若 GUI 进程还开着：它是旧代码（B帧+音频对齐未生效），重启后生效。
- 可删：`D:\qq下载\920\4_clipped.mp4`（417MB 无声拉伸半成品）；`.cache/enc_compare/`（~250MB 编码实验切片）。
